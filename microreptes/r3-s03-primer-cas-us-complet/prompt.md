# Prompt base del corrector

Avalua `r3-s03-primer-cas-us-complet` només amb evidencies del repositori.

Comprova que el flux està identificat com a heretat de `R2` i que funciona en el framework amb ruta, controlador o equivalent, model/servei/capa de dades, BBDD real, resposta visible i validacio o error minim.

Revisa si almenys este flux o un altre de `R3` és server-rendered amb vista o plantilla. Per a este microrepte, no exigisques segon flux ni API.

Penalitza pantalles estàtiques, dades fixes, tota la logica dins d'una ruta, absencia de BBDD real o falta de prova del cas positiu.
